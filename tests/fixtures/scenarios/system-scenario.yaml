propositions:
  - SystemAlert
  - PerformMaintenance
  - SendNotification
  - ScheduleBackup
  - ((SystemHealthy ∧ DatabaseConnected) → SystemAlert)
  - (BackupCompleted → ScheduleBackup)

steps:
  # STEP 1: (SystemHealthy ∧ DatabaseConnected)
  - rule: alpha
    subtype: and
    from:
      - SystemHealthy
      - DatabaseConnected

  # STEP 2: (SecurityScanPassed ∧ DiskSpaceAvailable)
  - rule: alpha
    subtype: and
    from:
      - SecurityScanPassed
      - DiskSpaceAvailable

targets:
  - (SystemHealthy ∧ DatabaseConnected)
  - ((SystemHealthy ∧ DatabaseConnected) → SystemAlert)
  - (SecurityScanPassed ∧ DiskSpaceAvailable)
