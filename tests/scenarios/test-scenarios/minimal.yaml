propositions:
  - SimpleAction

steps:
  # Minimal step: when no resolvers run, UserWantsEuropeanFlight is missing
  # and the step is recorded as skipped with reason 'missing_fact'.
  - rule: alpha
    subtype: and
    from:
      - UserWantsEuropeanFlight
      - SimpleAction

targets:
  - SimpleAction
