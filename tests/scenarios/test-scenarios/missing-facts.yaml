propositions:
  - ActionA
  - ActionB
  - (UserWantsEuropeanFlight → ActionA)

steps:
  # This step is skipped because NonExistentFact1, NonExistentFact2 are missing
  # and not referenced anywhere else, so they have no scope to be derived from.
  - rule: alpha
    subtype: and
    from:
      - NonExistentFact1
      - NonExistentFact2

targets:
  - (NonExistentFact1 ∧ NonExistentFact2)
  - ActionA
  - ActionB
